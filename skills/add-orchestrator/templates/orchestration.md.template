# {{SYSTEM_NAME}} — Orchestration

> The **narrative design** of this multi-agent system: the nodes, the edges (who calls
> whom and why), the permission boundaries, and the collaboration patterns. This is the
> **intent** layer.
>
> - Machine facts (who exists) live in `fleet/system-map.yaml`.
> - Enforced permissions live in the Trinity SystemManifest (`fleet/system.yaml`).
> - This file is the single standard home for the *narrative*. It is imported by
>   `CLAUDE.md` and loaded into context at session start.
>
> **Authorship:** prose sections are human/agent-authored — edit them freely. Blocks
> between `<!-- BEGIN GENERATED:* -->` and `<!-- END GENERATED:* -->` are rewritten by
> `/discover-agents` and `/compose-system` — do not hand-edit those.

## 1. System overview
<One paragraph: what this fleet is for and the through-line that connects the agents.>

## 2. Topology
<!-- BEGIN GENERATED:topology (written by /discover-agents from system-map + declared permissions) -->
```mermaid
graph LR
  %% nodes + edges auto-generated; empty until first scan
```
<!-- END GENERATED:topology -->

## 3. Nodes (roles)
<!-- BEGIN GENERATED:roster (written by /discover-agents) -->
| agent | role | does | ref |
| --- | --- | --- | --- |
<!-- END GENERATED:roster -->

### 3a. Role reality (interview-corrected)
<prose corrections from `/profile-fleet` or `/fleet-reconcile` when verified reality
contradicts the generated roster above — folded into the roster summaries on the next
`/discover-agents` scan; empty is the healthy state>

### 3b. Ownership matrix (RACI-lite — informational)
One fleet-wide matrix: for each recurring **domain of work**, who does it (R), who gives
input before it's done (C), who is told after (I). Accountability is structural, not a
column — the managing agent owns the record, the operator owns escalations.

**This is informational, not enforced.** Use it as routing and etiquette defaults:
prefer the domain's R when routing; ask C for input before finalizing significant
output; mention outcomes to I (digest or chat). Ambiguity — no R, or two Rs — is a
prompt to ask the operator, never a hard stop.

| domain | responsible (does it) | consulted (input before done) | informed (told after) |
| --- | --- | --- | --- |
<human-authored — one row per recurring domain of work; agent names must match §3>

### 3c. Data layer (canonical data — /add-canon, informational)
Where the fleet's **published truth** lives: agents that participate in the shared canon
repo declare it via `x-canon:` (surfaced as `canon:` per node in the system map). Reads
of published facts go to the canon folder (cited at `canon@<sha>`), not a chat turn;
writes always route to the owning agent — own-folder-only, cross-folder via PR.

| domain of fact | owning agent | canon folder | notes |
| --- | --- | --- | --- |
<human-authored — one row per canonical domain; empty until the fleet adopts /add-canon>

## 4. Edges — who calls whom (and why)
The allow/deny is enforced by the manifest; the *why* is narrative and lives here.

| caller | callee | trigger / when | channel (direct msg / shared folder / event / group) | rationale |
| --- | --- | --- | --- | --- |

## 5. Permissions & boundaries
Least-privilege intent: who MAY talk to whom, and what is explicitly denied. **This
section is the source `/compose-system` reads to derive `agent_permissions`.** Keep it
honest — do not grant an edge here you would not enforce.

## 6. Collaboration patterns (choreographies)
Named, reusable multi-agent flows.

### <pattern-name>
- **Trigger:**
- **Steps:** A → B → C
- **Human gate:**
- **Output:**

## 7. Escalation & human-in-the-loop
When does the system stop and ask a human? Who is the operator? Where do approvals go
(e.g. Trinity operator queue)?

## 8. Design invariants
- Orchestration is **agent-owned**; Trinity is substrate (permissions, shared folders,
  messaging, cron) and runs **no central DAG engine**.
- The Trinity `SystemManifest` is the only prescriptive format; this doc is its
  narrative source, not a competing schema.
- <system-specific invariants>

## 9. Maintenance
- Refresh nodes/topology: `/discover-agents`.
- Reconcile permission drift: re-run `/discover-agents` (it rewrites the topology from
  the **declared** permissions — `system.yaml` / §5; live `agent_permissions` aren't
  exposed over MCP), then re-run `/compose-system` + deploy to push §5 onto Trinity.
- Regenerate the manifest from §5 intent: `/compose-system`.
- Bring deployed agents to their GitHub HEAD (non-destructive, pull-only): `/sync-fleet-to-head`.
- Re-interview agents & correct this narrative against reality: `/profile-fleet`.
- Fold already-verified deltas (fixes applied, audit corrections) into this narrative
  and the other doc surfaces without new interviews: `/fleet-reconcile`.
- **Last reviewed:** {{DATE}}
